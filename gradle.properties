# Gradle / Kotlin (JVM-only build — ADR 0001).
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8

# Performance
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.parallel=true

# Kotlin
kotlin.code.style=official
