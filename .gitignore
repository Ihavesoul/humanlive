# OS
.DS_Store
Thumbs.db

# Node (PoC PWA tooling)
node_modules/
npm-debug.log*

# Env / secrets — never commit
.env
.env.*
!.env.example
*.local

# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android (machine-specific — never commit)
local.properties
*.iml
.idea/
.cxx/
captures/

# Kotlin
.kotlin/

# Durable SQLite store (local dev / test) — never commit a database
*.db
*.db-journal
*.sqlite

# Editor
.vscode/
*.swp
